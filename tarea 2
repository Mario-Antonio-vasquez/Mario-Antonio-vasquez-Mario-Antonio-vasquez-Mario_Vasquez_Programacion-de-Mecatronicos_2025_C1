#include <stdio.h>
#include <stdlib.h>



  /* Promedio Curso.
  El programa, al recibir como dato el promedio de un alumno a 6.

  PRO: variable de tipo real. */


  void main(void)
  {
      float PRO;
      printf("ingrese el promedio del alumno: ");
      scanf("%f", &PRO);
      if (PRO >= 6)
        printf("\nAprobado");
  }
